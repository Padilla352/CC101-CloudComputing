# Infrastructure Investigation Report

## Checkpoint 2 – Investigate the Cloud Server

This report documents the investigation of the Linux environment provided by KillerCoda. The Linux terminal was used to identify the operating system, kernel version, CPU, memory, storage, mounted file systems, hostname, and IP address.

## 1. Operating System

The operating system running in the KillerCoda environment is:

**Ubuntu 24.04.4 LTS**

Command used:

```bash
cat /etc/os-release
